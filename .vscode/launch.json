{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "attach",
            "pid": "${command:pickProcess}",
            "name": "Rust Debug Attach"
        }
    ]
}